<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
  <% if flash[:errors] && !flash[:errors].empty?  %>
   Sorry, the following issues have occured when you tried to sign up:
   <ul id='errors'>
    <% flash[:errors].each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>
  <% if current_user %>
  Welcome, <%= current_user.email %>
  <% end %>
  <%= yield %>
  </body>
</html>
